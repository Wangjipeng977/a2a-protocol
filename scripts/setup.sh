#!/bin/bash
# Setup script for a2a-protocol scripts/
# (validate_agent_card.py uses stdlib only — no install needed)

echo "✅ scripts/ ready — validate_agent_card.py is runnable directly"
echo "   Usage: python3 scripts/validate_agent_card.py <agent-card.json>"